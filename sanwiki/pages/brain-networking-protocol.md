# Brain Networking Protocol

![Brain Networking Protocol layer stack diagram](/v5ma.github.io/wiki/assets/generated/san/brain-networking-protocol/brain-networking-protocol-layer-stack-20260629-v1.png)

This diagram maps the page's four-layer protocol claim: oscillator carrier bands establish addressable channels, phasic bursts delimit symbol packets, phase-change events switch signals toward selected dendritic receivers, and attention plus attention-schema act as the control layer that decides which channels count.

**Brain Networking Protocol** is the term [[micah-blumberg|Micah Blumberg]] uses in the SAN source notebook `a0027z.md` to name the *protocol-shaped* set of conventions by which one [[selfaware-networks-hypothesis|self-aware network]] within a brain communicates with another — a shorthand for the [[oscillators|oscillator]]-mediated, [[phasic-burst|phasic-burst]]-timed signalling layer that carries [[attention|attention]] and [[attention-schema|attention-schema]] messages between cortical sub-networks. The single line of evidence in this packet establishes only that the phrase "Brain networking protocol" appears in `a0027z.md` ([[gh-a0027z|a0027z]]--md, line 29); the rest of this stub draws definitional consequences from that placement among the suggested neighbouring [[Concepts|concepts]].

Read against its suggested-wikilinks neighbourhood, the protocol is composed of four interlocking layers. The *carrier* layer is built from coupled [[oscillators]] whose [[power-bands|power-bands]] divide the spectrum into addressing channels — analogous to frequency-division multiplexing — so that distinct sub-networks can talk past each other without collision ([[gh-a0027z|a0027z]]--md, line 29). The *symbol* layer is built from [[phasic-burst-firing|phasic-burst firing]] events: timed packets of spikes that carry the actual content, with [[phasic-burst|phasic bursts]] acting as the delimiters that separate one transmission from the next. The *switching* layer is built from [[phase-change|phase-change]] events that route signals between [[dendritic-spines|dendritic spines]], promoting some spines into receivers and demoting others. The *control* layer is built from [[attention]] and [[attention-schema|attention-schema]] gates that decide which channels are listened to at all — the protocol's equivalent of a routing table.

In SAN's framing, the brain networking protocol is therefore not a metaphor borrowed from Internet protocols but a literal claim about how cortex coordinates: that the [[theory|theory]] of mind treats inter-network communication as oscillatory packet-switching with phase-change addressing and burst-coded payloads ([[gh-a0027z|a0027z]]--md, line 29). This makes the protocol a hinge between the local [[dendritic-spines|dendritic-spine]] biophysics and the network-level [[selfaware-networks-hypothesis|self-aware-networks]] picture: it is the layer that turns a soup of oscillators and bursts into something a higher-level [[attention-schema]] can model and report on. Until further evidence is backfilled, the page should be read as a definitional anchor pointing the reader at the four layers above and at the SAN [[Concepts|concepts index]] as the master gloss.

## Related concepts

- [[oscillators]] — the carrier-layer component the protocol rides on top of.
- [[power-bands]] — the frequency-division addressing scheme of the carrier layer.
- [[phasic-burst]] — the protocol's symbol-delimiter primitive.
- [[phasic-burst-firing]] — the firing mode that produces the symbol layer.
- [[phase-change]] — the routing/switching event that re-points signals between spines.
- [[dendritic-spines]] — the receivers/transmitters at the cellular endpoint of the protocol.
- [[attention]] — the control-layer gate selecting which channels are listened to.
- [[attention-schema]] — the meta-model the brain uses to represent its own [[attention]] state.
- [[Concepts]] — the SAN concepts index that glosses each layer.
- [[theory]] — Blumberg's umbrella SAN theory page in which this protocol is embedded.

## Source Provenance

- Generated: 2026-04-24T17:57:02
- Lane: C.draft (from packet)
- Packet: `wiki/automation/packets/brain-networking-protocol.json`
- Evidence count: 1
- Source ids: [[gh-a0027z|a0027z]]--md
  - `a0027z--md` line 29 → https://github.com/v5ma/selfawarenetworks/blob/d1c26a6fbf273695185a4036411215f53209eb71/a0027z.md

---

*Auto-generated draft (Lane C, deterministic synthesis from packet). Lane D will add `[[wikilinks]]`. Lane E (single-flight, manual) promotes to `wiki/reference/selfawarenetworks-legacy-wiki/`.*
