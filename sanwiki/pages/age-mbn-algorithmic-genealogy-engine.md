# Age Mbn Algorithmic Genealogy Engine

**Age Mbn Algorithmic Genealogy Engine** (AGE-MBN) is a named subsystem concept in the [[self-aware-networks]] theoretical vocabulary that describes a computational mechanism for tracing the causal and temporal lineage of memory patterns through a neural network. The name unpacks into three components: *Age* refers to the temporal dimension — how old a memory trace is relative to the current oscillatory cycle; *Mbn* (likely an abbreviation for Memory-Basis Network or Memory-Brain-Network) denotes the substrate of encoded patterns; and *Algorithmic Genealogy Engine* names the process of algorithmically constructing the inheritance tree — the genealogy — of how a given memory pattern was derived from earlier patterns through learning. This stub is derived solely from the title and its definitional consequences within the SAN theoretical vocabulary; no primary-source evidence has yet been extracted from the SAN GitHub corpus.

In the SAN framework the brain is fundamentally a [[brain-as-memory-prediction-machine]]: its primary operation is not raw sensory processing but the continuous update of a structured memory bank that encodes the world as a hierarchy of prediction templates. The AGE-MBN concept extends this by asking not just *what* is stored but *how* it got there — the provenance chain of each memory trace. This is closely related to [[ltp-structural-memory-encoding]] and [[ltp-bouton-synaptic-memory-traces]], which describe the physical substrate of memory consolidation via long-term potentiation: as synaptic connections are strengthened through repeated co-activation, they leave a structural record of the temporal ordering of that co-activation. The [[camkii-microtubule-ltp]] pathway and [[kibra-pkm-zeta-memory-anchor]] mechanisms ensure that older traces are stabilised at the molecular level, making them the root nodes in a genealogical tree whose leaves are more recently acquired associations. A [[galactic-filaments-ltp-fractal]] analysis suggests that this genealogical structure has fractal properties — the same branching pattern of memory derivation recurs at multiple spatial and temporal scales across the network.

The genealogy metaphor connects to several other SAN memory models. [[Oscillatory-convergence-memory]] describes the process by which distributed partial activations converge on a complete memory pattern, and the AGE-MBN framing would label each convergence event as a generational step — a child pattern derived from multiple parent activations. [[Oscillation-memory-reinstatement]] provides the dynamic by which an older root trace can re-express itself in the context of new input: when the current oscillatory phase matches the encoding phase of an old trace, that trace is reinstated, and the resulting pattern is a hybrid of old genealogy and new context. [[Memory-wave-recontextualization]] extends this further: each reinstatement event modifies the reinstated trace slightly, updating its genealogy by adding a new edge. [[Dendritic-spine-temporal-memory]] contributes the cellular-level record: individual dendritic spines that were potentiated during a specific learning episode retain the molecular timestamp of that episode, allowing the AGE-MBN algorithm to read the age of each synaptic connection as a date stamp in the genealogy. [[Oscillatory-temporal-memory-window]] defines the temporal scope within which the genealogy is actively maintained in working memory — traces outside this window are retired to long-term storage and can only be accessed via explicit retrieval cues.

The *algorithmic* component of the AGE-MBN name emphasises that genealogy reconstruction is not a passive readout but an active computational process. [[Brain-data-structure-query]] is the SAN concept that frames the brain as running structured queries over its memory substrate; AGE-MBN can be understood as one type of query — a *lineage query* that asks "what is the ancestral chain of this pattern?" rather than "what patterns match this cue?" This is closely related to [[self-activating-content-addressable-memory]], where a memory is addressed by its content rather than its location, and where retrieval automatically activates associated memories in order of genealogical proximity. [[Memory-as-regenerable-pattern]] captures a key consequence: a memory that can be regenerated from its genealogical ancestors need not be stored in full at every level of the hierarchy; instead, only the deltas relative to the parent pattern need to be stored, with the full trace reconstructed on demand. [[Belief-as-spatiotemporal-sequence]] generalises this: beliefs are sequences of patterns arranged along the temporal dimension of the genealogy, and the AGE-MBN engine is what maintains the sequential ordering that gives a belief its narrative structure.

The AGE-MBN concept also has implications for how the brain handles novel input. [[Neural-drift-virtual-engrams]] describes the phenomenon whereby a memory trace shifts over time as new experiences are encoded — the engram "drifts" through the weight space. The AGE-MBN genealogy would record this drift as a directed path through the memory-pattern space, with each drift step labelled by the triggering experience. [[Neural-sequence-representation]] and [[neuronal-sequences-population-bursts]] provide the population-level correlates: a genealogically ordered sequence of memory patterns corresponds to a temporally ordered sequence of population bursts, with each burst in the sequence being a generational descendant of the preceding one. [[Sharp-wave-ripples]] during sleep are thought to replay these sequences in compressed form, a process that AGE-MBN would interpret as genealogy consolidation — pruning redundant branches and strengthening canonical lineage paths. [[Dreams-as-theta-wave-sequences]] likewise represent a slower, more exploratory genealogy traversal during REM sleep, generating novel pattern combinations by following unusual edges in the genealogy graph. [[Theta-oscillations-background-scripts]] maintain the background rhythm within which these traversal operations proceed, and [[oscillatory-memory-invariance]] ensures that the genealogical identity of a pattern is preserved across different oscillatory contexts.

## Related concepts

- [[brain-as-memory-prediction-machine]] — the foundational SAN framing within which AGE-MBN operates
- [[ltp-structural-memory-encoding]] — the synaptic substrate recording the genealogy of each memory trace
- [[oscillatory-convergence-memory]] — the convergence events that generate new nodes in the genealogy tree
- [[memory-wave-recontextualization]] — recontextualisation as genealogy update
- [[self-activating-content-addressable-memory]] — content-addressable memory whose access pattern follows genealogical proximity
- [[memory-as-regenerable-pattern]] — memories stored as deltas relative to genealogical parents
- [[neural-drift-virtual-engrams]] — drift as directed movement through the genealogy graph
- [[sharp-wave-ripples]] — sleep-phase genealogy consolidation events
- [[brain-data-structure-query]] — the query interface over the memory substrate that AGE-MBN extends
- [[belief-as-spatiotemporal-sequence]] — beliefs as genealogically ordered pattern sequences
- [[oscillation-memory-reinstatement]] — the dynamic by which root-node traces re-express in new contexts
- [[kibra-pkm-zeta-memory-anchor]] — molecular-level anchoring of root nodes in the genealogy

## Source Provenance

- Generated: 2026-04-24T18:17:02
- Lane: C.draft (from packet)
- Packet: `wiki/automation/packets/age-mbn-algorithmic-genealogy-engine.json`
- Evidence count: 0
- Source ids: 

---

*Auto-generated draft (Lane C, deterministic synthesis from packet). Lane D will add `[[wikilinks]]`. Lane E (single-flight, manual) promotes to `wiki/reference/selfawarenetworks-legacy-wiki/`.*
