# Power Of Two Permutation Logic

**Power Of Two Permutation Logic** is a concept name that appears as a referent in other [[gh-a0206z|Self]]-Aware Networks pages but for which the Lane B evidence sweep returned zero primary-source quotations in the github clone. The literal name combines two well-defined ideas — *power of two* in the sense of 2ⁿ scaling, and *permutation logic* in the combinatorial sense — but the SAN-specific reading of the conjunction is not yet documented from a primary passage.

## Origin

The packet for this slug (`wiki/automation/packets/power-of-two-permutation-logic.json`) is an evidence-empty packet: `evidence_count: 0`, `source_count: 0`, with no `chronology_markers.earliest_year`. The concept is referenced from elsewhere (it appears as a wikilink target with at least two dead links pointing to it, per the packet's `dead_link_count: 2`), but no primary passage in the SAN github clone matches the slug's match-phrase set (`Power Of Two Permutation Logic`, `power of two permutation logic`, `power-of-two-permutation-logic`, `power_of_two_permutation_logic`). Because the Lane C+ contract forbids inventing citations, the body cannot legitimately state what [[gh-resnote04|Micah]] meant by the term until a primary passage is acquired.

## Position

In the absence of a primary citation, the term can only be read against its literal components. *Power of two* is the family 2ⁿ — the doubling sequence (1, 2, 4, 8, 16, 32, …) familiar from binary [[information-theory|information theory]] and from the dyadic frequency spacing implied by SAN's [[brain-rhythms]] discussion. *Permutation logic* refers to the combinatorial mappings between ordered configurations — in [[category-theory|category theory]] terms, the symmetric-group action on a finite set. A "power-of-two permutation logic" therefore most naturally names a permutation system whose state space is sized 2ⁿ, of which the canonical instance is the action on n binary degrees of freedom. Whether SAN uses the term in that strictly combinatorial sense, or as shorthand for a specific neural mechanism, requires a primary passage to confirm and is not asserted here.

## Chronology

Not establishable from the empty packet. No transcript, paper, or note in the SAN github clone for the indexed permalink matches the term. If a future evidence-acquisition pass surfaces one, this section can be re-populated.

## Related concepts

- [[information-theory]] — power-of-two state spaces are the natural setting for binary [[information-theory|information theory]].
- [[category-theory]] — permutation logic is, formally, the symmetric-group action on a finite set, naturally framed in category-theoretic terms.
- [[concepts]] — the concept exists as a referent in other SAN pages and is recorded here as a stub pending primary evidence.

## Source Provenance

- Generated: 2026-04-24T18:12:00
- Lane: C.draft (from packet)
- Packet: `wiki/automation/packets/power-of-two-permutation-logic.json`
- Evidence count: 0
- Source ids: 

---

*Auto-generated draft (Lane C, deterministic synthesis from packet). Lane D will add `[[wikilinks]]`. Lane E (single-flight, manual) promotes to `wiki/reference/selfawarenetworks-legacy-wiki/`.*
